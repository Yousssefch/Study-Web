import React from "react";
import { ReactDOM } from "react";
import './style.css';

var minutes = Math.floor((20 % (1000 * 60 * 60)) / (1000 * 60));
document.getElementById()
function Timer() {
    return(
        <div className="timer">
        <h1 className="text-timer" id="Countdown">20:00</h1>
        </div>

    )

}
export default Timer;